# SMARTBIOFIT - Configurações Docker
# ===========================================

# Configurações do Banco de Dados (Docker)
DB_HOST=db
DB_PORT=3306
DB_NAME=smartbiofit
DB_USER=smartbiofit_user
DB_PASS=smartbiofit123
DB_ROOT_PASSWORD=root123

# Configurações da Aplicação (Docker)
APP_NAME=SMARTBIOFIT
APP_ENV=development
APP_DEBUG=true
APP_URL=http://localhost:8080
APP_URL_LOCAL=http://localhost:8080
APP_URL_EXTERNAL=http://localhost:8080
APP_TIMEZONE=America/Sao_Paulo

# Configurações de Segurança
JWT_SECRET=SmartBioFit2025SecretKey123!@#
SESSION_LIFETIME=1440
HASH_ALGO=sha256

# Configurações de Upload
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,pdf,mp4,avi

# Configurações de Email (para recuperação de senha)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@smartbiofit.com
MAIL_FROM_NAME=SMARTBIOFIT

# Configurações de Logs
LOG_LEVEL=debug
LOG_PATH=logs/

# Configurações de Cache
CACHE_ENABLED=true
CACHE_LIFETIME=3600